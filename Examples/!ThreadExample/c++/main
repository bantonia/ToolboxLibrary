#include "ThreadExample.h"

ThreadExample *task;

int main() {
  task = new ThreadExample();
  task->initTask("<ThreadExample$Dir>");
  task->setMask(256 + 1);
  while (!task->quit) {
    task->run();
  }
  delete task;
  return 0;
}
