The Application !CodeTemplate sources have been generated by !Prototype which can be found in the Tools directory within this Repository.

!Prototype is at a very early stage and uses the ToolboxC++Lib, currently it only generates main and other supporting sources to extend the class ToolboxTask.