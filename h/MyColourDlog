#include "ToolboxColourDlogObj.h"

class MyColourDlog : public ToolboxColourDlogObj {
  public:
    MyColourDlog();
    MyColourDlog(ObjectId id);
    ~MyColourDlog();
    void aboutToBeShownHandler(ToolboxEvent *event, IdBlock *id_block, void *handle);
    void dialogueCompletedHandler(ToolboxEvent *event, IdBlock *id_block, void *handle);
    void colourSelectedHandler(ToolboxEvent *event, IdBlock *id_block, void *handle);
    void showProperties() {};
};
